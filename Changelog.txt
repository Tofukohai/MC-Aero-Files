## 🐛 Fixes

- Fixed "Failed to decode packet clientbound minecraft:command" crash on join — removed `connectivity` mod which conflicted with `Packet Fixer` (both patched the same NeoForge mixins, causing some packet size limit patches to be skipped; Packet Fixer alone now handles all packet size fixes)